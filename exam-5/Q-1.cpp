#include <iostream>

using namespace std;

int main(){
     int arr , size , max ;

    cout << "enter the size of array : ";
    cin >> size;

    for(int  i=0; i < size; i++ ){
        cout << "enter the value : " ;
        cin >> arr;
       
    }
    for(int  i=0; i <size; i++ ){
        cout << arr << " ";  
    }
   for (int  i = 0; i < size; i++)
   {
    if(size != 0 && size < 0);
    cout << "negative num is";
   }
   

    return 0;
}